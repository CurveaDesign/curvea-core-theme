{%- liquid
  assign gap = gap | default: 'gap-4'
  assign classes = classes | default: ''
-%}

{%- if content != blank -%}
  <div class="flex flex-col {{ gap }} {{ classes }}">
    {{ content }}
  </div>
{%- endif -%}
